# 共同空间验收（2026-09-12）

入口 http://127.0.0.1:4197/ 。独立loopback服务与持久目录；4196纸桥实验保留。
样例不调用模型、不读取.env。人物仅为身份标记，未宣称角色美术完成。

## 已实现

6×5米程序化室内剖面、4件家具、3D/2D布局；纸桥位于来源关联的展示架。
本人要求、门口/活动区/家具几何冲突、经历纠正与重核、完整方案版本选择、个人下一步和自报、Markdown导出。
只有成功存储的命令才返回；序号冲突409、相同请求幂等，角色绑定拒绝代替另一人更改要求。
当前两名固定合成角色的所有内容彼此共享，不接生产个人数据。

## 本次证据

- 17项Python通过：13领域、4真实HTTP/持久服务测试。
  覆盖几何、本人权限、版本过期、经历纠正/撤回、严格输入、导出、独立store/service重开、
  幂等、过期写入、写失败不发布、旧lab端点隔离及撤销后请求拒绝。
  本轮未另做操作系统级崩溃恢复实验。
- 1440×1100与390×844双浏览器Context实际通过：
  邀请加入 -> 各自要求 -> 冲突同步 -> B解除冲突 -> 分别接受 ->
  移动使同意过期 -> 2D -> 经历纠正和重核 -> 本人行动/自报 ->
  下载Markdown -> 另一端刷新恢复 -> 伪造他人要求被拒。
  pageerror为空，手机无横向溢出。
- 独立渲染检查通过：3D选中拖动、2D真实拖动、390px触摸选择、20次apply保持mesh UUID、
  失焦/丢失指针取消拖动并恢复、不发送移动命令、dispose移除canvas。
- 主站及样例生产构建通过；样例JS593.34kB、gzip152.65kB，大包提示保留。
- 原后端331通过、1跳过、149条既有警告。生产数据和原站逻辑没有修改。

证据目录：C:/Users/Li Hao/AppData/Local/Temp/meetmind-space-evidence/
含report.json、initial-desktop.png、conflict-desktop.png、plan-desktop.png、mobile.png、summary.md。
截图不展示访问令牌，报告不存令牌。独立渲染图在服务器/tmp/shared-space-renderer-*.png。
约55次绘制、1.08万三角形、1个1024阴影；未测低端真机/网络，不能当全设备性能结论。

## 视觉与范围

暖调木地板、开窗剖面、倒角家具、软包、书籍和植物使任务可辨识。
修正了窄屏相机裁切、过小标签和纸桥画在错误家具上。
这不是AAA世界或人物验收，测试数量不代表达到用户原始品质要求。

当前是[共同空间规格](SHARED-SPACE-EXPERIMENT.md)的首段实现。
自由家具、尺寸/预算/妥协编辑、完整行动时间/完成标准、模型提议和实验分组尚未完成。
B是人工布局样例，不是AI求解；没有真实用户拥有感、任务收益或付费证据。
CHAT账户Arrearage未解决，本轮没有重试付费调用。
